<%= form_for(@second_model) do |f| %>
  <% if @second_model.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@second_model.errors.count, "error") %> prohibited this second_model from being saved:</h2>

      <ul>
      <% @second_model.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :description %><br />
    <%= f.text_area :description %>
  </div>
  <div class="field">
    <%= f.label :number %><br />
    <%= f.text_field :number %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
